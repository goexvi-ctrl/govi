# Build output
/bin/

# Compiled binaries (make, go build, accidental package builds)
/govi
/bridge
/cmd/govi/govi
/cmd/govi/goex
/gui/bridge/bridge

# engine/version.go is generated from engine/version.gox + the Version file by
# `make` (see Makefile), but it is checked in so `go build ./cmd/govi` works
# without make. Only the maintainer regenerates it when bumping the Version.

# macOS GUI app build products (libgovi archive, linked binary, .app bundle)
/gui/build/

# Conformance oracle: the compiled C nvi binary, fetched/built locally.
/internal/conformance/oracle/nvi

# Editor/OS cruft
*.swp
.DS_Store

# Test scratch (large-file generation, recovery temp dirs)
/testdata/large/
/vi.recover

# make coverage / make coverage-html
/cover.out
/cover.html

.aider*

# Ignore docs we don't plan to check in
/docs/qwen-review
/docs/issues.md
/docs/selectable-text-plan.md
/docs/unified-selection-plan.md
/Tasks.md
/colors_mapping.txt
/docs/VI_EX_COMMANDS.md

/PERF_PLAN.md

# Release artifacts: built dmgs live on the GitHub releases page, not in git.
/GoVi-*.dmg

# ctags index for local navigation
/tags
/mkrel

/qa
